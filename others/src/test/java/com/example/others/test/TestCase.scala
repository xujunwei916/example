package com.example.others.test

import java.text.SimpleDateFormat
import java.util.Base64.Decoder

object TestCase {

    def main(args: Array[String]): Unit = {
//        val array = Array("aaaa",1)
//       val  Array( a, b) = array
//        println(a)
//        println(b)
//        val sdf=new SimpleDateFormat("yyyyMMdd")
        ////        val date= sdf.parse("2018-01-01")
        ////        println(date)

//        val sqlClick = "select t3.`date` as date,t3.`hour` as hour,t3.adsenseid as aid,t3.dspid as dspid ,count(1) as pv from " +
//            "(select t1.bidid,t1.adsenseid,t1.dspid,t1.adid,t1.`date`,t1.`hour` from  click_info t1 where t1.`date`='" + date + "') t3 " +
//            "left join " +
//            "(select distinct t2.bidid,t2.aid,t2.dspid,t2.adid from bid_history_v3 t2 where t2.`date` ='" + date + "' or t2.`date` ='" + preDate + "') t4 " +
//            "on  t3.bidid = t4.bidid and t3.adsenseid = t4.aid and t3.dspid = t4.dspid and t3.adid =t4.adid " +
//            "where t4.bidid is not null " +
//            "group by  t3.`date`,t3.`hour`, t3.adsenseid,t3.dspid"
//        val sql = "select t3.`date` as date,t3.`hour` as hour,t3.adsenseid as aid,t3.dspid as dspid ,count(1) as pv from " +
//            "(select t1.bidid,t1.adsenseid,t1.dspid,t1.adid,t1.`date`,t1.`hour` from  impression_info t1 where t1.`date`='" + date + "') t3 " +
//            "left join " +
//            "(select distinct t2.bidid,t2.aid,t2.dspid,t2.adid from bid_history_v3 t2 where t2.`date` ='" + date + "' or t2.`date` ='" + preDate + "') t4 " +
//            "on  t3.bidid = t4.bidid and t3.adsenseid = t4.aid and t3.dspid = t4.dspid and t3.adid =t4.adid " +
//            "where t4.bidid is not null " +
//            "group by  t3.`date`,t3.`hour`, t3.adsenseid,t3.dspid"

//        println(sqlClick)
//        println(sql)

    }

}
